// faça o quadro de memória e mostre a saida na tela para o código abaixo

#include<stdio.h>

int main(){
    int num = 0;

    while(num < 4){ // enquanto o numero for menor que 4, ele vai...
        printf("%d", num++);
        num += 2;
    }
    // printar o primeiro numero que é = 1
    // depois adicionar +2 ou seja = 3
    // ai vai printar 4 e depois o programa para
}
