#include<stdio.h>

// entrada: razao, primeiro termo
// processamento: a10 = a0 + razao * 9 ou ai = a0 + razao * (i - 1)
// saida: a10

int main(){
    float tInicial, razao, n, termoN;

    scanf("%f %f %f", &tInicial, &razao, &tFinal);

    termoN = tInicial + razao * (n - 1);

    printf("\n%.2f", result);

    /* scanf("%f %f", &razao, &termo);

    termo10 = termo + razao * 9;

    printf("%.2f", termo10); */
}
