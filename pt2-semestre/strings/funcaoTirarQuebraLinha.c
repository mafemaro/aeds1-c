/* void tirarQuebraDeLinha(char *string, int tamanho) {
    fgets(string, tamanho, stdin);
    int tam = strlen(string);
    if (tam > 0 && string[tam - 1] == '\n') {
        string[tam - 1] = '\0';
    }
} */